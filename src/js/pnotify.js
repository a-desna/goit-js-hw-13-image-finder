'use strict';

export const { notice, success, error, defaults } = require('@pnotify/core');
defaults.delay = 1500;
defaults.animateSpeed = 'normal';
defaults.styling = 'material';
defaults.icons = 'material';
